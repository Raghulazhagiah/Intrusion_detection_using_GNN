Intrusion Detection using Graph Neural Networks (GNNs)
This project applies a hub-centric graph approach where each IP acts as a hub node connecting related traffic records, reducing graph density while preserving structure. A 4-layer SAGEConv model achieved 80% accuracy on the UNSW-NB15 dataset.
